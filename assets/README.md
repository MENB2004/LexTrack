# LexTrack App Launcher Assets Folder

Place your standalone launcher app icon PNG image here and name it `icon.png` (dimensions 1024x1024 pixels recommended).

This asset will be automatically compiled as the APK launcher icon and splash screen by EAS Build based on the paths configured in `app.json`.
